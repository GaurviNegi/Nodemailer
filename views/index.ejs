<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>NodeMailerApp - Email App</title>
  <link
    href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700&display=swap"
    rel="stylesheet"
  />
  <link rel="stylesheet" href="style.css" />
</head>
<body>
  <div class="container">
    <h1 class="title">Nodemailer App</h1>
    <p class="description">send emails using nodemailer. fill the form and click on send-email to send the email.</p>
    <form action="/send-email" method="POST" class="email-form">
     
        <input type="email" name="email" id=email placeholder="enter recepient email" required>
     
        <textarea name="message" id="message" cols="30" rows="10"  placeholder="enter the message."></textarea>

       <button class="button">send-email</button>
   </form>

 
   <% if (typeof status !== 'undefined' && status && typeof message !==
   'undefined' && message) { %>
   <div class="status-message <%= status %>"><%= message %></div>
   <% } %>

  <p class="copyright">&copy; 2023 NodeMailerApp. All rights reserved.</p>
  </div>

</body>
</html>